[package]
name = "web_ss_synth"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
core_ss_synth = { path = "../core_ss_synth", default-features = false, features = ["no_std_core"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
